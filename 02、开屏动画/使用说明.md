# 使用说明 · 站点结构与开屏动画

> 站点：**支援未来**（https://monarchish.github.io）
> 本文说明整个仓库的编排规则，以及开屏动画每个文件是干什么的。
> 最后更新：2026-09-22（仓库重编排为 01 / 02 / 03 编号 + 中文文件名）
> 动画本体来自开源项目 [Nyaasu66/ark-act54-web](https://github.com/Nyaasu66/ark-act54-web)（Unity 解包数据还原），
> 仅供个人学习装饰，素材版权归原作者所有。

---

## 一、仓库长什么样

```
Monarchish.github.io/                 ← 仓库根
│
├─ index.html                         ★ 站点入口（浏览器约定的首页名，不能改名）
│
├─ 01、支援未来/                       ← 你的文档正文，改内容只动这里
│  ├─ 首页.md                         首页（点左上角「支援未来」回到这里）
│  ├─ 01.准入/  01.01.准入、01.02.MDG
│  ├─ 02.采购/  02.01.订货 ~ 02.05.同步
│  ├─ 03.销售/  03.01.让步单 ~ 03.10.采购发票
│  ├─ 04.财务/  04.01 ~ 04.06
│  └─ 05.其他/  05.01 ~ 05.07
│
├─ 02、开屏动画/                       ← 站点打开时的开场动画（平时不用动）
│  ├─ index.html                      动画页入口（被主页用 iframe 盖住播放）
│  ├─ 1.动画播放器.js                  动画的大脑：逐帧求值 + 合成画面
│  ├─ 2.曲线求值.js                    动画曲线的数学求值器（Hermite 插值）
│  ├─ 3.动画数据.js                    游戏资源解包出的图层树 + 曲线（约 350KB，别手改）
│  ├─ 4.行为配置.js                    ★ 想改动画行为就改这个
│  ├─ 5.动画页样式.css                 动画页自己的样式（画布缩放）
│  ├─ 6.主页挂载.js                    主页侧：建 iframe、播完淡出覆盖层
│  ├─ 7.主页覆盖层样式.css              主页侧：覆盖层的样式与淡出动画
│  ├─ 使用说明.md                      本文件
│  └─ 素材/贴图/                       38 张 png（动画画面素材）
│
└─ 03、设置/                           ← 主页的功能代码（站点"设置"）
   ├─ 1.站点主程序.js                  导航清单 + 文档渲染
   └─ 2.站点样式.css                   主页外观样式
```

---

## 二、命名规则（以后加东西照着来）

| 位置 | 规则 | 例子 |
|---|---|---|
| 一级文件夹 | **两位数字 + 顿号 + 中文名** | `01、支援未来`、`04、XX` |
| 文件夹内的技术文件 | **序号 + 中文名 + 原扩展名** | `1.动画播放器.js`、`2.站点样式.css` |
| 网页入口 | 固定叫 `index.html`（浏览器约定，访问目录时自动打开） | 根目录、02 各一个 |
| 文档正文 | 沿用原编号 | `03.09.00.销售开票.md` |

> **为什么数字要两位？** 一位数会在有第 10 个文件夹时排错：「10、」会跑到「2、」前面。
> 两位（01、02……10、11）永远按顺序排，加到 99 都不乱。

---

## 三、一次访问发生了什么

```
访客打开 monarchish.github.io
   │
   ├─ index.html 里的一小段脚本先判断：这次要不要播动画？
   │     （同一会话只播一次；?intro=0 不播，?intro=1 强制重播）
   │
   ├─ 要播 → 02、开屏动画/6.主页挂载.js 建一个铺满全屏的 iframe
   │     └─ iframe 加载 02、开屏动画/index.html → 1.动画播放器.js 开始播放
   │
   ├─ （动画播放期间，主页的侧边栏和正文在覆盖层底下并行加载，
   │    所以动画一结束，主页已经是"就绪"状态，几乎秒开）
   │
   ├─ 动画播到 3.79 秒（黑圆「眼睛」转场最黑的一帧）
   │     └─ 动画页给主页发一条消息 {type:"done"}
   │
   └─ 6.主页挂载.js 把覆盖层淡出（0.6s）并删除 → 露出「支援未来」主页
```

---

## 四、02、开屏动画 里每个文件干什么

| 文件 | 是什么 | 谁在用 | 要不要动 |
|---|---|---|---|
| `index.html` | 动画页入口。只负责搭舞台，然后加载播放器 | 被 6.主页挂载.js 以 iframe 加载 | 基本不用动 |
| `1.动画播放器.js` | **动画的大脑**。读动画数据 → 按帧求值 → 写 DOM 图层；播到收场点通知主页 | 被 index.html 加载 | 不用动 |
| `2.曲线求值.js` | **数学工具**。把关键帧用 Hermite 插值算成任意时刻的值 | 被 1.动画播放器.js 引用 | 永远不用动 |
| `3.动画数据.js` | **动画数据**。从游戏资源解包导出的图层树 + 全部曲线（390 帧 / 60fps / 总长 6.5s） | 被 1.动画播放器.js 动态加载 | **不要手改** |
| `4.行为配置.js` | **行为配置**。停在第几秒、藏不藏活动主界面、素材 CDN 地址 | 被 1.动画播放器.js 引用 | ★ 改动画就改它 |
| `5.动画页样式.css` | 动画页的样式：1920×1080 画布等比缩放（手机竖屏自动转 9:16） | 被 02 的 index.html 引入 | 一般不动 |
| `6.主页挂载.js` | **主页侧的挂载脚本**：建 iframe、监听消息、淡出覆盖层、超时兜底 | 被根目录 index.html 引入 | 一般不动 |
| `7.主页覆盖层样式.css` | 覆盖层的样式（盖住主页、淡出动画、锁滚动） | 被根目录 index.html 引入 | 一般不动 |
| `素材/贴图/` | 38 张 png，动画的画面素材 | 由 3.动画数据.js 里的路径引用 | 同名替换即可 |

---

## 五、想改东西，改哪儿

| 我想…… | 改这个文件 |
|---|---|
| 改收场时间（停在别的秒数） | `02、开屏动画/4.行为配置.js` → `redirectAtSeconds` |
| 每次进站都播动画（不只在首次） | `02、开屏动画/6.主页挂载.js` → `oncePerSession: false` |
| 以后彻底不播动画 | 根目录 `index.html` → 删掉 `intro-on` 那段判断脚本（临时用 `?intro=0` 即可看主页） |
| 新增 / 删除文档 | `03、设置/1.站点主程序.js` → 顶部 `sidebarManifest` 清单 |
| 改主页配色、排版 | `03、设置/2.站点样式.css` |
| 换动画里的某张图 | 用同名文件覆盖 `02、开屏动画/素材/贴图/` 里的图 |
| 调动画页在手机上的表现 | `02、开屏动画/5.动画页样式.css` |

---

## 六、4.行为配置.js 配置项

| 配置项 | 当前值 | 作用 |
|---|---|---|
| `redirectUrl` | `"../index.html"` | 只在**单独打开** `/02、开屏动画/index.html` 时生效（跳回主页）。挂在主页里播时用不到 |
| `redirectAtSeconds` | `3.79` | 播到第几秒收场。3.75 是「眼睛」最清晰的一帧，3.79 是最黑的一帧；最大 6.5（完整剪辑） |
| `fadeOutMs` | `500` | 独立打开时的黑场淡出时长。挂在主页时由 6.主页挂载.js 负责淡出，此项不生效 |
| `hideFrontUi` | `true` | 隐藏剪辑自带的活动菜单 / 顶栏 / 仿游戏 HUD。**保持 true**，否则结尾会闪出游戏菜单 |
| `assetCdnBase` | jsDelivr 地址 | 素材 CDN 加速（国内访问 github.io 直连很慢）。填 `""` 可关闭；改动素材后 CDN 最长 12 小时才更新 |
| `enableDebugQuery` | `true` | 允许用网址参数临时覆盖上面的配置 |

**6.主页挂载.js 顶部也有一个 CONFIG**：`oncePerSession`（是否每次会话只播一次）、
`fadeOutMs`（覆盖层淡出 600ms，要和 7.主页覆盖层样式.css 里的 transition 一致）、
`loadTimeoutMs` / `playTimeoutMs`（卡住时的兜底收场时间，防止主页被卡死）。

---

## 七、调试用网址参数

| 参数 | 作用 |
|---|---|
| `?intro=0` | 本次不播动画，直接看主页 |
| `?intro=1` | 强制重播（忽略"本次会话已播过"） |
| `?debug=1` | 在控制台打印播放流程 |
| `?t=3.5` | （动画页）定格在第 3.5 秒，逐帧核对画面 |
| `?autoplay=0` | （动画页）不自动播放 |
| `?redirect=0` | （动画页）播完不跳转 |

---

## 八、关于素材瘦身（2026-09-22）

动画只播到 3.79 秒，后面的活动主界面与结尾主视觉都不显示。
因此用无头浏览器逐帧（60fps）实测了 0–3.79 秒**真正显示过**的图片：
原 92 张里只有 36 张在用，其余 54 张（活动界面按钮、玻璃反光、闪光贴片、水波帧等）
已连同它们的加载一起删除——**少了 54 个请求**，国内访问时载入明显变快，
画面经像素级 MD5 比对与改动前完全一致。另外 `sprite_white$0.png`（纯色块）和
`title_main_mask.png`（标题遮罩）虽然不在逐帧结果里，但被渲染逻辑直接引用，已保留。

> ⚠️ 如果以后改成播完整 6.5 秒（把 `redirectAtSeconds` 调大），需要把删掉的 54 张图
> 从 git 历史里找回来，并清空 `1.动画播放器.js` 里的 `SKIP_SPRITE_FILES` 清单。
> 被删的文件在 git 历史里都还在。

---

## 九、旧链接怎么办

文件夹改名后，旧地址（例如 `/三、开屏动画/`）会 404，这是正常现象：
站点内部所有引用都已同步改成新名字，只有**外部收藏的旧链接**会失效。
如果哪天希望旧地址也能用，可以在仓库里加一个跳转页，随时说一声。
