/* ============================================================
   开屏动画 · 配置文件
   ------------------------------------------------------------
   精简版：无声音、无跳过按钮、无播放控制条。
   动画播到「眼睛」黑圆转场（约 3.79s）→ 黑屏 → 淡入主页。
   ============================================================ */

export const INTRO_CONFIG = {
  /* ---------------------------------------------------------
     1. 单独打开本页时的跳转目标
     平时挂在主页（index.html）里播放时不会用到这一项；
     只有直接访问 /三、开屏动画/ 时才生效，默认跳回站点主页。
     --------------------------------------------------------- */
  redirectUrl: "../index.html",

  /* ---------------------------------------------------------
     2. 播放到第几秒收场（秒）
     3.75s = 「眼睛」黑圆 + 绿色月牙完全成形；
     3.79s = 月牙收拢、画面最黑的一帧（3.8s 起主视觉进场）。
     在 3.79s 定格 → 黑场 → 淡入主页，衔接最像游戏自身的转场。
     想停在「眼睛」清晰可见的一帧就改成 3.75。
     --------------------------------------------------------- */
  redirectAtSeconds: 3.79,

  /* 收场前的黑场淡出时长（毫秒）。0 = 硬切。
     挂在主页时由 overlay.js 负责淡出，这一项不会生效。 */
  fadeOutMs: 500,

  /* ---------------------------------------------------------
     3. 隐藏剪辑自带的活动主界面与结尾主视觉之后的内容
     true  = 只播到黑圆转场，后面的菜单/主界面一律不渲染
     false = 原样渲染整段剪辑
     --------------------------------------------------------- */
  hideFrontUi: true,

  /* ---------------------------------------------------------
     4. 素材 CDN 加速（大陆访客实测有效）
     GitHub Pages 在国内加载很慢，100 多张图片经常超时。
     图片/数据文件优先走 CDN，全部失败自动回退到本站同源文件。
     - 想关闭 CDN 加速：填 ""（空字符串）
     - 注意：改动素材后 CDN 最长缓存 12 小时才更新
     --------------------------------------------------------- */
  assetCdnBase: "https://gcore.jsdelivr.net/gh/Monarchish/Monarchish.github.io@main/三、开屏动画/",

  /* ---------------------------------------------------------
     5. 调试参数：在网址后面临时追加，不修改上面的配置
       ?t=3.5        定格在第 3.5 秒（不自动播放），逐帧核对画面
       ?autoplay=0   不自动播放
       ?embed=0      强制按"独立页面"运行（即使被主页嵌入也不通知主页）
       ?embed=1      强制按"被主页嵌入"运行
       ?redirect=0   关闭跳转（只想看动画时用）
       ?at=3.0       临时改收场时间点
     --------------------------------------------------------- */
  enableDebugQuery: true,
};
